pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               sh 'pwd'
               sh 'ls'
                sh 'terraform init'
                sh 'terraform plan'
                sh 'terraform apply --auto-approve'
            }
        }

       }
   }

