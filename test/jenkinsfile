pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
             dir ('test') {
             
               sh 'pwd'
               sh 'ls'
                sh 'terraform init'
                sh 'terraform plan'
                sh 'terraform apply --auto-approve'
      }
            }
        }

       }
   }

